# Environment Configuration
When executing Python commands, ALWAYS use the interpreter located at:
`.\.venv\Scripts\python.exe` (on Windows) or `./.venv/bin/python` (on Linux/Mac).

Do not run `python` directly from the global path.
Activate the virtual environment in `.venv` before running any scripts.
